<Window x:Class="KooliProjekt.WpfApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KooliProjekt.WpfApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>
        
        <DataGrid x:Name="EmployeesDataGrid" 
                  Grid.Column="0"
                  CanUserAddRows="False"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Data}"
                  SelectedItem="{Binding SelectedItem}"
                  IsReadOnly="True"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*" />
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1" Margin="20">
            <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,10">Employee Details</TextBlock>
            
            <TextBlock Margin="0,5,0,2">ID</TextBlock>
            <TextBlock Text="{Binding SelectedItem.Id}" FontWeight="Bold" Margin="0,0,0,10"></TextBlock>
            
            <TextBlock Margin="0,5,0,2">First Name</TextBlock>
            <TextBox Text="{Binding SelectedItem.FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"></TextBox>
            
            <TextBlock Margin="0,5,0,2">Last Name</TextBlock>
            <TextBox Text="{Binding SelectedItem.LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"></TextBox>
            
            <TextBlock Margin="0,5,0,2">Email</TextBlock>
            <TextBox Text="{Binding SelectedItem.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"></TextBox>
            
            <TextBlock Margin="0,5,0,2">Phone</TextBlock>
            <TextBox Text="{Binding SelectedItem.Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"></TextBox>
            
            <TextBlock Margin="0,5,0,2">Role</TextBlock>
            <TextBox Text="{Binding SelectedItem.Role, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"></TextBox>

            <StackPanel 
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,20,0,0">
                <Button Command="{Binding NewCommand}" Margin="5" Padding="15,5">New</Button>
                <Button Command="{Binding SaveCommand}" Margin="5" Padding="15,5">Save</Button>
                <Button Command="{Binding DeleteCommand}" Margin="5" Padding="15,5">Delete</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
